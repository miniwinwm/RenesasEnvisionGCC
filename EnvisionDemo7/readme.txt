This demonstration shows how to use a compare match timer.

Drivers are included going straight to I/O registers.

A compare/match timer is set to run and match at 20 Hz. In the
match interrupt the user LED's state is toggled.